# Add these environment variables to your production environment

# For Puppeteer in production (optional - only if you have a specific Chrome installation)
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome

# For debugging PDF generation
DEBUG_PDF_GENERATION=true

# Force fallback mode (set to true if Puppeteer consistently fails)
FORCE_FALLBACK_PDF=false
